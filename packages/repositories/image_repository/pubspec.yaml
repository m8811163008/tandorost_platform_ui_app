name: image_repository
description: A library for managing user images, including reading, adding, archiving, and retrieving image details.
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.0

# Add regular dependencies here.
dependencies:
  remote_api:
    path: ../../data/remote_api

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0

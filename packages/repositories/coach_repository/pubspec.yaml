name: coach_repository
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.0

# Add regular dependencies here.
dependencies:
  remote_api:
    path: ../../data/remote_api
  local_storage:
    path: ../../data/local_storage

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

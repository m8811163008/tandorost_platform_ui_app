import 'package:flutter/widgets.dart';

class ResultRoute extends StatelessWidget {
  const ResultRoute({super.key});
  static const String name = 'result_route';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
